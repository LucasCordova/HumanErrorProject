@model DataVisuals.LineChart

<div class="container bg-light my-3 pt-3 pb-5 px-5 shadow-sm">
    <div class="mx-auto text-center">
        <h2 class="display-4">
            Method Declaration Progress Over Snapshots
        </h2>
    </div>
    <div class="row justify-content-center align-items-center">
        <div id="@Model.Id" class="w-100 h-300px"></div>
    </div>
</div>
@await Html.PartialAsync("Shared/Charts/_LineChartPartial", Model)